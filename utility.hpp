#pragma once

namespace NUtility
{
    enum class EColor
    {
        UNDEFINED,

        GREEN,
        RED,
    };

    const float err = 0.00001;
    const int dimensions = 2;

    const int width = 600;
    const int height = 400;
}

